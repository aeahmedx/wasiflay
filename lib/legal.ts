/**
 * Bump this when the terms change in a way people should re-read. Every
 * signed-in account is re-prompted, and each profile records the version
 * it accepted — so there's an answer to "what did they actually agree
 * to" for any given person.
 *
 * Format is the date the wording changed.
 */
export const TERMS_VERSION = "2026-08-20";

export const SUPPORT_EMAIL = "wasiflay@gmail.com";

/** Youngest permitted account. Enforced at signup by the age gate. */
export const MINIMUM_AGE = 13;
